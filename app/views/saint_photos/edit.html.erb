<%= render 'layouts/regular_sidebar' %>
<div id="page-content-wrapper">
  <%= render 'shared/menu_toggle' %>
  <h1>Editing Saint Photo</h1>

  <%= render 'form', saint_photo: @saint_photo %>

  <%= link_to 'Show', @saint_photo %> |
  <%= link_to 'Back', saint_photos_path %>
</div>
<!-- /.page-content-wrapper -->
<%= render 'shared/top_of_page' %>
<%= render 'shared/side_menu_script' %>
