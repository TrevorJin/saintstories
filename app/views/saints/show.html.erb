<div class="container-fluid">
	<div class="row">
		<div class="col-xl-12 column">
			<div class="tabbable" id="tabs-444468">
				<ul class="nav nav-tabs">
					<li class="nav-item">
						<a href="#panel-200304" data-toggle="tab" class="nav-link active">General</a>
					</li>
					<!-- /.active nav-item -->
					<% if @saint.timeline_events.any? %>
						<li class="nav-item">
							<a href="#panel-567649" data-toggle="tab" class="nav-link">Life Timeline</a>
						</li>
						<!-- /.nav-item -->
					<% end %>
					<% if @following_saints.any? %>
						<li class="nav-item">
							<a href="#panel-987654" data-toggle="tab" class="nav-link">Related Saints</a>
						</li>
						<!-- /.nav-item -->
					<% end %>
					<% if @saint.written_works.any? %>
						<li class="nav-item">
							<a href="#panel-123456" data-toggle="tab" class="nav-link">Written Works</a>
						</li>
						<!-- /.nav-item -->
					<% end %>
					<% if @saint.saint_photos.any? %>
						<li class="nav-item">
							<a href="#panel-777777" data-toggle="tab" class="nav-link">Photos</a>
						</li>
						<!-- /.nav-item -->
					<% end %>
				</ul>
				<!-- /.nav nav-tabs -->
				<div class="tab-content">
					<div class="tab-pane active" id="panel-200304">
						<div class="row" style="margin-left: 0; margin-right: 0;">
            	<div class="col-xl-3 push-md-8 column">
            		<% if !@saint.avatar.blank? %>
            			<% if @saint.avatar_alternative_text? %>
            				<%= image_tag @saint.avatar.url, class: "rounded-circle saint-image", alt: @saint.avatar_alternative_text, title: @saint.avatar_alternative_text %>
            			<% else %>
            				<%= image_tag @saint.avatar.url, class: "rounded-circle saint-image" %>
            			<% end %>
            		<% end %>
            		<%= render 'saints/info_sidebar' %>
            	</div>
            	<!-- /.col-xl-3 push-md-8 column -->
            	<div class="col-xl-8 pull-md-3 column saint-info">
            		<h1>
									<%= title @saint.name %>
								</h1>
								<% if @saint.long_description.present? %>
									<%= @saint.long_description.html_safe %>
								<% end %>
            	</div>
            	<!-- /.col-xl-8 pull-md-3 column saint-info -->
            </div>
            <!-- /.row -->
            <% if logged_in? && current_user.admin? %>
            	<%= render 'saints/admin_saint_controls' %>
            <% end %>
					</div>
					<!-- /.tab-pane active -->
					<% if @saint.timeline_events.any? %>
						<div class="tab-pane" id="panel-567649">
    					<div class="row" style="margin-left: 0; margin-top:20px; margin-right: 0;">
    						<%= render 'saints/saint_timeline' %>
    					</div>
    					<!-- /.row -->
						</div>
						<!-- /.tab-pane -->
					<% end %>
					<% if @following_saints.any? %>
						<div class="tab-pane" id="panel-987654">
							<%= render 'saints/saint_related_saints' %>
						</div>
						<!-- /.tab-pane -->
					<% end %>
					<% if @saint.written_works.any? %>
						<div class="tab-pane" id="panel-123456">
    					<div class="row" style="margin-left: 0; margin-right: 0;">
        				<div class="section section-dark text-center landing-section" style="margin-left: 0; margin-right: 0;">
									<%= render 'saints/saint_written_works' %>
								</div>
								<!-- /.section section-dark text-center landing-section -->
    					</div>
    					<!-- /.row -->
						</div>
						<!-- /.tab-pane -->
					<% end %>
					<% if @saint.saint_photos.any? %>
						<div class="tab-pane" id="panel-777777">
    					<div class="row" style="margin-left: 0; margin-right: 0;">
								<%= render 'saints/saint_photo_page' %>
    					</div>
    					<!-- /.row -->
						</div>
						<!-- /.tab-pane -->
					<% end %>
				</div>
				<!-- /.tab-content -->
			</div>
			<!-- /.tabbable -->
		</div>
		<!-- /.col-xl-12 column -->
	</div>
	<!-- /.row -->
</div>
<!-- /.container-fluid -->
